const Loading = () => {
  return (
    <div className="Loader">
      <img
        alt="logo"
        className="loader-img img-fluid"
        id="logoImage"
        src="/loader.gif"
      />
    </div>
  );
};

export default Loading;
